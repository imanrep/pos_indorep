import 'package:fluent_ui/fluent_ui.dart';
import 'package:pos_indorep/web/model/member_model.dart';

class BeveragesBuyDialog extends StatelessWidget {
  const BeveragesBuyDialog({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink(); // does nothing
  }
}

class MemberTopupDialog extends StatelessWidget {
  final Member member;
  const MemberTopupDialog({super.key, required this.member});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink(); // does nothing
  }
}

class NewTopupDialog extends StatelessWidget {
  const NewTopupDialog({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink(); // does nothing
  }
}
